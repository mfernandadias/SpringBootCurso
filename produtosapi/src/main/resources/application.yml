spring:
  application:
    name: Produtos API
  datasource:
    url: jbdbc:h2:mem:produtos
    username: sa
    password: password
  jpa:
    database-pataform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
      path: /h2-console
