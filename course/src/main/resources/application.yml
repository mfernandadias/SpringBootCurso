spring:
  application:
    name: Produtos API
  datasource:
    url: jdbc:h2:mem:produtos
    username: fe
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
      path: /h2-console
    datasource:
      url: jdbc:h2:mem:produtos;DB_CLOSE_DELAY=-1
      username: sa
      password:
